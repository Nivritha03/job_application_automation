# Instahyre selectors
APPLY_BTN = "button.apply-btn, button.btn-apply, a.apply-btn, button.applyBtn, button[class*='apply-btn'], a[class*='apply-btn']"
ALREADY_APPLIED = "button:has-text('Applied'), span:has-text('Applied'), div:has-text('already applied')"
MODAL = ".modal-dialog, div[role='dialog']"
SUBMIT_MODAL = "button:has-text('Submit'), button:has-text('Confirm'), button:has-text('Send Application')"
EXTERNAL_APPLY_BTN = "button:has-text('Apply on company site'), button:has-text('Apply on Company Website'), a[target='_blank'][href*='apply']"
