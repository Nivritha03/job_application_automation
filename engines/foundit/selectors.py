# Foundit selectors
# Foundit's primary apply button classes: "apply-btn", "btn-apply", "quick-apply"
APPLY_BTN = "button.apply-btn, a.apply-btn, button.btn-apply, button[class*='apply-btn'], button:has-text('Quick Apply')"
ALREADY_APPLIED = "button:has-text('Applied'), span:has-text('Applied'), div:has-text('already applied'), div:has-text('Application Submitted')"
MODAL = "div.apply-modal, div.ia-Modal, div[role='dialog']"
SUBMIT_MODAL = "button:has-text('Submit'), button:has-text('Confirm'), button:has-text('Apply Now')"
EXTERNAL_APPLY_BTN = "button:has-text('Apply on company site'), button:has-text('Apply on Company Website'), a[href*='apply']:not([href*='foundit.in'])"
