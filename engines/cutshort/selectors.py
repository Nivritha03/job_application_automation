# Cutshort selectors
# Cutshort's apply button is typically class-based or has specific text like "Apply Now"
APPLY_BTN = "button.apply-btn, a.apply-btn, button[class*='applyButton'], button[class*='apply-btn'], button:has-text('Apply Now')"
ALREADY_APPLIED = "button:has-text('Applied'), span:has-text('Applied'), div:has-text('already applied')"
CHAT_INPUT = "textarea[placeholder*='message'], textarea[placeholder*='type'], [id*='chat-textarea']"
SEND_BTN = "button:has-text('Send'), button[class*='send']"
EXTERNAL_APPLY_BTN = "button:has-text('Apply on company site'), button:has-text('Apply on Company Website'), a[href*='greenhouse.io'], a[href*='lever.co'], a[href*='ashbyhq.com']"
